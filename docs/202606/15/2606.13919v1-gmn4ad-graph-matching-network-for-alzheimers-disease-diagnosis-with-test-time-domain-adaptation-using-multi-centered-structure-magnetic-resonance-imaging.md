---
title: "GMN4AD: Graph Matching Network for Alzheimer's Disease Diagnosis with Test-Time Domain Adaptation using Multi-centered Structure Magnetic Resonance Imaging"
title_zh: GMN4AD：基于图匹配网络的阿尔茨海默病诊断与测试时域自适应方法在多中心结构磁共振成像中的应用
authors: "Chen Zhao, Huan Huang, Yixin Xie, Jiajing Huang, Weihua Zhou, Nandakumar Narayanan"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13919v1"
tags: ["query:fmri-brain-network"]
score: 8.0
evidence: 图匹配网络，阿尔茨海默诊断，结构MRI
tldr: 针对阿尔茨海默病诊断中多中心sMRI数据存在模态和站点异质性问题，提出GMN4AD模型。该模型利用图匹配网络捕获异质脑图间的跨图关系，并引入测试时域自适应策略结合对比学习缓解域偏移。在三个公开数据集上，GMN4AD优于现有方法，为AD诊断提供了鲁棒且泛化的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13919v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1705, \"height\": 1132, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13919v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1244, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13919v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 668, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13919v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 602, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13919v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1664, \"height\": 1229, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13919v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1713, \"height\": 750, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13919v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1711, \"height\": 363, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13919v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 460, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13919v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1679, \"height\": 847, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13919v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1727, \"height\": 1143, \"label\": \"Table\"}]"
motivation: 传统图方法难以处理多中心sMRI数据的异质性，导致诊断性能受限。
method: 提出图匹配网络GMN4AD，建模异质脑图间交互，并采用测试时域自适应与对比学习。
result: 在三个公开AD数据集上，GMN4AD性能优于现有最先进方法。
conclusion: GMN4AD为AD诊断提供了鲁棒且泛化的解决方案。
---

## 摘要
阿尔茨海默病（AD）是一种进行性神经退行性疾病，影响数百万老年人，其患病率在未来几年预计将显著上升。早期诊断，尤其是在轻度认知障碍（MCI）阶段，对于及时干预至关重要。结构磁共振成像（sMRI）已成为检测AD相关脑部变化的关键模态，但传统的基于图的方法往往难以应对模态和站点间的异质性，限制了诊断性能。本文提出了用于阿尔茨海默病诊断的图匹配网络（GMN4AD），旨在建模从神经影像数据中导出的异质性脑图之间的交互。与将每个脑图独立处理的传统方法不同，GMN4AD利用图匹配来捕获跨图关系，从而提升诊断精度。此外，我们引入了一种测试时域自适应策略，结合对比学习来减轻推理过程中的域偏移。在三个公开AD数据集上的大量实验表明，与最先进的方法相比，GMN4AD实现了优越的性能，为AD诊断提供了一种鲁棒且可泛化的解决方案。

## Abstract
Alzheimer's Disease (AD) is a progressive neurodegenerative disorder that affects millions of older adults, with prevalence expected to rise significantly in the coming years. Early diagnosis, particularly during the mild cognitive impairment (MCI) stage, is critical for timely intervention. Structural Magnetic Resonance Imaging (sMRI) has emerged as a key modality for detecting AD-related brain changes, but traditional graph-based approaches often struggle with modality and inter-site heterogeneity, limiting diagnostic performance. In this paper, we propose Graph Matching Network for Alzheimer's Disease Diagnosis (GMN4AD), designed to model interactions between heterogeneous brain graphs derived from neuroimaging data. Unlike conventional methods that treat each brain graph independently, GMN4AD leverages graph matching to capture cross-graph relationships, enhancing diagnostic precision. Furthermore, we introduce a test-time domain adaptation strategy that combines contrastive learning to mitigate domain shifts during inference. Extensive experiments on three public AD datasets demonstrate that GMN4AD achieves superior performance compared to state-of-the-art methods, offering a robust and generalizable solution for AD diagnosis.

---

## 论文详细总结（自动生成）

好的，以下是对论文《GMN4AD: Graph Matching Network for Alzheimer's Disease Diagnosis with Test-Time Domain Adaptation using Multi-centered Structure Magnetic Resonance Imaging》的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：阿尔茨海默病（AD）的早期诊断至关重要，结构磁共振成像（sMRI）是检测AD相关脑部变化的关键手段。然而，传统基于图神经网络（GNN）的方法在处理来自不同中心、不同扫描仪和不同采集协议的sMRI数据时，面临严重的**模态异质性和站点间异质性**问题。这导致模型在跨中心数据上的泛化能力差，诊断性能受限。
- **研究动机**：现有方法通常将每个受试者的脑图视为独立个体，未能有效建模不同脑图之间的结构对应关系，也难以应对训练域（源域）和测试域（目标域）之间的数据分布差异（域偏移）。
- **整体含义**：本文旨在解决上述问题，提出一个鲁棒且可泛化的AD诊断框架，能够有效处理多中心sMRI数据的异质性，提升跨中心诊断的准确性。

### 2. 论文提出的方法论：核心思想、关键技术细节

论文提出了一个名为 **GMN4AD** 的图匹配网络，其核心思想是**通过图匹配来量化不同受试者脑图之间的结构相似性**，并利用**测试时域自适应（TTA）** 策略来缓解域偏移。

**核心流程分为三个阶段：**

1.  **脑图构建**：
    - 从sMRI数据中，使用FreeSurfer分割出感兴趣区域（ROIs）作为图的节点。
    - 基于ROI质心之间的空间关系构建边。
    - 为每个节点提取113维的放射组学特征（包括强度、纹理、形状等），形成节点特征向量。

2.  **图匹配网络（GMN4AD）**：
    - **图内特征嵌入**：使用图卷积网络（GCN）和多层感知机（MLP）对单个脑图内的节点特征进行编码，捕获局部拓扑和区域依赖关系。
    - **跨图特征嵌入**：这是核心创新点。通过迭代计算两个图之间的节点-节点亲和力矩阵（使用Sinkhorn算法进行归一化），并基于此亲和力进行信息传播，使模型能够学习两个图之间的结构对应关系。
    - **全局特征提取与相似度计算**：通过图池化操作将每个图压缩为全局表示向量，然后使用余弦相似度计算两个图的相似度得分。该得分用于判断两个受试者是否患有相同的AD阶段。

3.  **测试时域自适应（TTA）**：
    - **动机**：模型在源域（如ADNI）训练后，直接应用于目标域（如AIBL）时性能会下降。
    - **方法**：在推理阶段，对目标域的无标签数据进行在线微调。通过引入随机特征扰动（高斯噪声和边丢弃）生成增强图，并计算对比损失，使模型学习到对域变化鲁棒的、不变的特征表示。该过程无需目标域标签。

**关键公式/算法流程（文字说明）：**
- **图内嵌入**：`z_i^(l+1) = MLP( [sum(z_j^l), z_i^l] )`，其中`z_i`是节点i的特征，`l`是GCN层数。
- **跨图亲和力**：`S_ij = Sinkhorn( exp( (z_i^1)^T * A_intra * z_j^2 ) / sqrt(d_intra) )`，计算图1节点i和图2节点j的匹配得分。
- **跨图嵌入**：`z_i^(m+1) = MLP( [sum(S_ij * z_j^m), z_i^m] )`，基于亲和力得分聚合来自另一个图的信息。
- **图相似度**：`s_hat = cos_sim( z_pool^1, z_pool^2 )`，计算两个图池化后的全局向量余弦相似度。
- **TTA损失**：`L_TTA = L_con_src + L_con_tgt`，其中`L_con`是原始图与增强图之间的对比损失。

### 3. 实验设计：数据集、基准与对比方法

- **数据集**：使用了三个公开的多中心AD数据集：
    - **ADNI** (ADNI1, ADNI2, ADNI Go): 774名患者，3880次sMRI扫描。
    - **AIBL**: 443名参与者，642次sMRI扫描。
    - **OASIS3**: 1474名参与者，选取了部分数据构建平衡数据集。
- **任务场景**：在三个数据集上分别进行三类二分类任务：
    - AD vs. CN (认知正常)
    - AD vs. MCI (轻度认知障碍)
    - MCI vs. CN
- **基准与对比方法**：
    - **无域自适应对比**：在单个数据集内（如ADNI）进行训练和测试，对比了GCN、GAT、GIN、FIL等图模型，以及一个去除了跨图嵌入模块的基线模型（BL）。
    - **域自适应对比**：以ADNI为源域，AIBL和OASIS3为目标域，对比了：
        - **SOI** (Source-Only Inference): 源域模型直接应用于目标域。
        - **DAGE**: 基于图嵌入的自监督域自适应方法。
        - **MEMO**: 基于单测试点边际熵最小化的测试时自适应方法。

### 4. 资源与算力

- **文中未明确说明**：论文中提到了使用PyTorch 1.13实现，但**没有提及具体的GPU型号、数量或训练时长**等算力信息。这是一个常见的缺失信息点。

### 5. 实验数量与充分性

- **实验数量**：实验非常充分。
    - **单数据集性能**：在ADNI、AIBL、OASIS3三个数据集上，分别进行了3种分类任务，共9组实验（表3）。
    - **方法对比**：在ADNI数据集上，与4种图模型和1个基线模型进行了对比，共15组实验（表4）。
    - **域自适应性能**：以ADNI为源域，在AIBL和OASIS3两个目标域上，与3种方法进行了对比，共18组实验（表5）。
    - **消融分析**：通过对比基线模型（BL）验证了跨图嵌入模块的有效性。
    - **可解释性分析**：进行了特征重要性和ROI重要性分析。
- **充分性与公平性**：
    - **充分**：实验覆盖了多个数据集、多种任务和多种对比方法，验证了模型在不同场景下的性能。
    - **客观公平**：论文声称对所有对比方法都进行了网格搜索和5折交叉验证，这保证了比较的公平性。实验结果以均值±标准差的形式呈现，体现了统计稳定性。

### 6. 论文的主要结论与发现

- **GMN4AD性能优越**：在单数据集（如ADNI）的AD vs. CN任务中，GMN4AD的准确率（0.9237）和特异性（0.9474）显著优于GCN、GAT等传统图模型。
- **TTA策略有效**：提出的TTA策略在跨数据集泛化任务中，普遍优于SOI、DAGE和MEMO方法，尤其是在AD vs. CN和CN vs. MCI任务上，准确率和特异性均有提升，证明了其在缓解域偏移方面的有效性。
- **可解释性发现**：
    - **关键特征**：纹理特征（如GLSZM）和强度分布特征（如Skewness）是区分AD阶段最重要的放射组学特征。
    - **关键脑区**：右侧脑血管、左侧脑血管和脑白质高信号区域是模型决策中最关键的ROI，这与AD的血管病理学和白质完整性丧失的已知知识相符。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：
    - **图匹配用于AD诊断**：将图匹配网络引入AD诊断，通过显式建模跨图结构对应关系，比独立处理每个图的传统方法更具信息量。
    - **测试时域自适应**：将TTA策略应用于图匹配任务，有效解决了多中心数据带来的域偏移问题，且无需目标域标签，实用性强。
- **实验设计**：
    - **多数据集验证**：在三个不同规模、不同人口统计学特征的公开数据集上进行验证，增强了结论的泛化性。
    - **全面的对比**：不仅对比了多种图模型，还对比了多种域自适应方法，实验设计全面。
    - **可解释性分析**：通过ZORRO-Paired方法提供了模型决策的神经生物学解释，增加了模型的可信度。

### 8. 不足与局限

- **计算效率**：推理阶段需要将每个测试样本与模板集中的所有样本进行图匹配，计算成本较高。论文本身也承认这是一个局限性。
- **任务限制**：当前模型仅支持二分类（如AD vs. CN），无法同时区分所有诊断类别（AD、MCI、CN）。未来工作需扩展到多分类。
- **模态单一**：仅使用了sMRI数据，未整合PET等其他模态信息，可能限制了诊断信息的全面性。
- **算力信息缺失**：未提供训练和推理所需的计算资源（GPU型号、时间等），不利于其他研究者复现和评估其资源需求。
- **性能波动**：在某些任务（如OASIS3上的MCI vs. CN）上，模型的特异性很高但敏感性很低，表明模型在识别MCI患者方面可能存在困难，存在类别不平衡或特征不明显的风险。

（完）
