---
title: "RegionFM: Interpretable Region-Based Brain MRI Classification Using Foundation Model Embeddings"
title_zh: "RegionFM: 基于区域的可解释脑MRI分类方法——利用基础模型嵌入"
authors: Wei Zhang
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16325v1"
tags: ["query:fbn"]
score: 8.0
evidence: 使用基础模型嵌入的可解释脑MRI分类
tldr: 脑MRI基础模型预测缺乏解剖可解释性。RegionFM通过解剖分割每个区域，用冻结基础模型提取区域嵌入，再用区域加性逻辑模型组合，使每个区域贡献标量项。在认知障碍分类中性能与微调方法相当，且提供解剖解释。随机嵌入消融实验验证预测依赖有意义结构，而非统计特征。该方法弥合模型解释与临床推理的差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16325v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1241, \"height\": 166, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16325v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 231, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16325v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 763, \"height\": 149, \"label\": \"Table\"}]"
motivation: 脑MRI基础模型预测难以用解剖结构解释，而临床评估依赖于区域异常。现有解释方法仅产生体素级重要性图，无法量化区域贡献。
method: RegionFM将MRI分割为解剖区域，用冻结基础模型编码每个区域为嵌入，通过区域加性逻辑模型组合，使每个区域输出一个标量贡献值。
result: 在认知障碍分类任务中，RegionFM性能与微调方法持平，随机嵌入消融实验性能接近随机，证明预测依赖于有意义的嵌入结构。
conclusion: RegionFM在保持竞争力的同时，提供了与解剖对齐的可解释性，更好支持临床推理。
---

## 摘要
基础模型为脑MRI分析提供了强大的表示能力，但其预测结果在解剖学意义上仍难以解释。临床脑MRI评估通常围绕解剖学定义的结构和区域异常展开，而传统的解释方法通常生成体素或斑块级别的重要性图，无法明确量化单个脑区域的贡献。为解决这一不匹配问题，我们提出了RegionFM，一个将解剖分割与脑MRI基础模型嵌入相结合的可解释框架。RegionFM首先将每个MRI扫描划分为解剖区域，并为每个区域构建单独的MRI体积。然后，一个冻结的基础模型将每个区域编码为嵌入向量，一个区域加性逻辑模型将这些嵌入组合起来，使得每个解剖区域对最终预测贡献一个显式的标量项。这种形式支持对区域贡献进行个体层面和群体层面的分析。我们使用多个预训练的脑MRI基础模型的嵌入，在认知障碍分类任务上评估了RegionFM。结果表明，RegionFM保持与可解释性较差的微调方法相当的性能，同时提供基于解剖学的解释。随机嵌入消融实验产生接近随机的性能，表明预测依赖于基础模型嵌入捕获的有意义结构，而非简单的特征统计。总体而言，RegionFM在保持竞争性预测性能的同时，使模型解释更符合基于解剖学的临床推理。

## Abstract
Foundation models provide powerful representations for brain MRI analysis, but their predictions remain difficult to interpret in anatomically meaningful terms. Clinical assessment of brain MRI is commonly organized around anatomically defined structures and regional abnormalities, whereas conventional explanation methods typically produce voxel- or patch-level importance maps that do not explicitly quantify the contributions of individual brain regions. To address this mismatch, we propose RegionFM, an interpretable framework that integrates anatomical segmentation with brain MRI foundation-model embeddings. RegionFM first divides each MRI scan into anatomical regions and constructs a separate MRI volume for each region. A frozen foundation model then encodes each region into an embedding, and a region-additive logistic model combines these embeddings such that every anatomical region contributes an explicit scalar term to the final prediction. This formulation supports both subject-level and cohort-level analyses of regional contributions. We evaluate RegionFM on cognitive-impairment classification using embeddings from multiple pretrained brain MRI foundation models. The results show that RegionFM maintains performance comparable to less interpretable fine-tuning approaches while providing anatomically grounded explanations. Randomized embedding ablations yield near-chance performance, indicating that the predictions rely on meaningful structure captured by the foundation-model embeddings rather than simple feature statistics. Overall, RegionFM better aligns model explanations with anatomy-based clinical reasoning while maintaining competitive predictive performance.

---

## 论文详细总结（自动生成）

# 详细中文总结：RegionFM

## 1. 论文的核心问题与整体含义（研究动机和背景）
- 脑MRI领域的基础模型虽能提取强大表示，但其预测缺乏解剖学意义上的可解释性。
- 临床脑MRI评估通常以**解剖结构**和**区域异常**为核心（如萎缩、心室扩大、白质高信号等），而现有解释方法（如体素/斑块重要性图）无法**显式量化单个脑区域**对预测的贡献。
- 这种**解释单元与临床推理之间的不匹配**是本文要解决的核心问题。
- 论文提出**RegionFM**，一个将**解剖分割**与**基础模型嵌入**相结合的可解释框架，旨在使模型预测能直接分解为每个解剖区域的标量贡献，从而桥接临床推理与模型解释。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- 将每个MRI扫描按解剖区域分割，对每个区域单独提取基础模型嵌入，再用**区域加性逻辑模型**组合所有区域嵌入，使得每个区域输出一个显式的标量贡献值，最终预测等于全局偏置加上所有区域贡献之和。

### 关键技术细节
1. **解剖区域定义**：使用FastSurfer（基于深度学习的神经影像分割工具）对原始MRI扫描进行解剖分割，得到K个解剖区域标签（如皮层区域、皮层下核团等）。需将分割标签映射到与基础模型预处理后MRI相同的空间。
2. **区域MRI构建**：对每个受试者 i 和每个区域 k，构建区域特定MRI体积：
   - 公式：**Xi,k = Mi,k ⊙ Xi + (1 − Mi,k) ⊙ ci**，其中Mi,k为二值掩膜，ci为受试者i的全局平均强度（作为背景填充值）。
   - 保留目标区域，非目标区域用平均强度填充，避免空白背景影响基础模型编码。
3. **基础模型嵌入提取**：使用**冻结的**预训练脑MRI基础模型（如BrainIAC、NeuroFM）编码每个区域MRI体积，得到嵌入向量 **zi,k = fθ(Xi,k)**，所有区域的嵌入组成受试者级表示。
4. **区域加性分类**：
   - 每个区域 k 拥有独立的线性层：**si,k = wk⊤ zi,k + bk**（区域特定参数，跨受试者共享）。
   - 受试者级logit：**hi = b0 + Σ si,k**，其中b0为全局偏置。
   - 预测概率：**ŷi = σ(hi)**，损失函数为二分类交叉熵。
5. **可解释性**：对每个受试者 i，**si,k** 直接衡量区域 k 对预测的影响（正贡献推正向正类，负贡献推反向负类）。支持：
   - 受试者级解释：按 si,k 大小排序区域重要性。
   - 群体级解释：在正确分类病例中统计各区域出现在top贡献的频率。

## 3. 实验设计
### 数据集与场景
- **OASIS-1** 结构脑MRI数据集，二分类任务：**认知正常 vs 认知障碍**。
- 数据划分：训练集、验证集、测试集（具体数量未提及）。
- 评价指标：**测试集ROC-AUC**，报告5个不同随机种子的均值和标准差。

### 基础模型（作为特征提取器）
- **BrainIAC**（来自Nat. Neurosci. 2026）和 **NeuroFM**（来自MedrXiv 2026），两者编码器均**冻结**使用。

### 对比方法
- 对应基础模型的全模型微调（**BrainIAC fine-tuning** 和 **NeuroFM fine-tuning**），作为可解释性较差的基线。

### 消融实验
- **随机嵌入消融**：验证模型是否依赖嵌入中的有任务相关信息。
  - **标准高斯消融**：每个嵌入值独立从标准正态分布采样。
  - **特征高斯消融**：每个嵌入维度从原始该维度的均值和标准差采样的高斯分布采样（保留特征边际统计，打乱区域-受试者关联）。
  - 两种消融均使用相同的区域加性分类器和训练流程。

## 4. 资源与算力
- **论文未明确说明**使用的GPU型号、数量、训练时长等信息。从方法描述（冻结基础模型+训练少量线性层）推断算力需求较低，但无法量化。

## 5. 实验数量与充分性
### 数量
- 主要实验：2个基础模型（BrainIAC、NeuroFM）× 2种方法（RegionFM vs 微调） = 4组对比。
- 消融实验：2组（标准高斯、特征高斯），均基于BrainIAC嵌入？论文未明确指定，但从上下文看可能基于BrainIAC。
- 总计约6组实验，每个实验5个随机种子。

### 充分性评价
- **不够充分**：
  - 仅使用**一个数据集**（OASIS-1），且任务仅为**二分类**，缺乏多分类、回归或纵向预测验证。
  - 未比较其他可解释方法（如Grad-CAM、SHAP、LIME等），也未验证区域加性模型相对于常见可解释分类器（如逻辑回归基于全图嵌入）的优势。
  - 消融实验设计较好，验证了预测依赖有意义结构，但未探讨不同背景填充值（平均强度 vs 零填充 vs 其他）的影响。
  - 未进行临床验证（如与放射科医生判断的一致性分析）。
- **客观性**：使用标准分割和评价指标，报告均值和标准差，对比公平（相同基础模型、相同数据划分），但数据集较小（OASIS-1样本量约几百），结果可能存在偏差。

## 6. 论文的主要结论与发现
1. **性能可比**：RegionFM + BrainIAC嵌入的ROC-AUC为 0.751±0.083，接近BrainIAC微调（0.759±0.089）；RegionFM + NeuroFM嵌入为 0.717±0.082，优于NeuroFM微调（0.645±0.088）。说明区域分解不会显著损失预测性能。
2. **解剖可解释**：RegionFM天然提供每个区域的标量贡献，支持受试者级和群体级解释。
3. **预测依赖有意义结构**：随机嵌入消融实验性能接近随机（标准高斯0.475±0.107，特征高斯0.505±0.117），证明预测依靠嵌入中的真实结构信息，而非边际统计或噪声。

## 7. 优点
- **解剖对齐**：解释单位与临床评估的解剖区域一致，提高了可解释性。
- **简单高效**：仅需区域加性线性层，训练成本低，且可应对冻结基础模型（无需微调整个大型网络）。
- **设计严谨**：嵌入了区域MRI构建（非目标区域用平均强度填充），避免背景信息干扰；消融实验提供了因果证据。
- **双层次解释**：支持个体和群体分析，适用范围广。

## 8. 不足与局限
- **实验覆盖不足**：仅使用一个数据集（OASIS-1，老龄化相关认知障碍），未验证在其他脑疾病（如肿瘤、中风、多发性硬化）或不同模态（如fMRI、DTI）上的泛化性。
- **任务单一**：仅二分类，未延伸到多分类、回归（如认知评分）或时序预测。
- **分割依赖**：依赖FastSurfer分割质量，错误分割会传播到后续步骤；且区域数量固定，无法自适应提取病态不规则区域。
- **背景填充假象**：使用平均强度填充非目标区域，可能引入非自然信号，基础模型可能对填充模式过敏感（虽消融表明仍依赖结构，但未专门分析）。
- **缺乏临床验证**：未与放射科医生或临床标准（如Hippocampal评分、白质高信号体积）进行对比，解释标签（si,k）的临床可接受性未经检验。
- **缺乏与其他可解释性方法比较**：未与Grad-CAM、注意力掩膜、概念瓶颈等比较可解释质量和性能。
- **算力信息缺失**：无法评估方法对计算资源的最低要求。

（完）
