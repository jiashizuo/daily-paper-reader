---
title: "RLKD: Distilling LLMs’ Reasoning via Reinforcement Learning"
title_zh: RLKD：通过强化学习蒸馏大语言模型的推理能力
authors: "Shicheng Xu, Liang Pang, Yunchang Zhu, Jia Gu, Zihao Wei, Jingcheng Deng, Feiyang Pan, Huawei Shen, Xueqi Cheng"
date: 2026-03-17
pdf: "https://ojs.aaai.org/index.php/AAAI/article/download/40710/44671"
tags: ["query:mlr"]
score: 6.0
evidence: 基于强化学习的教师LLM推理蒸馏
tldr: 针对监督微调蒸馏仅复制表面推理路径的问题，提出RLKD，利用强化学习从教师LLM中蒸馏隐含的多分支推理结构。通过RL优化学生模型对推理子问题选择和求解的学习，在多个推理基准上超越了传统蒸馏方法。
source: AAAI-2026-Accepted
selection_source: conference_retrieval
motivation: 监督微调蒸馏只能获取表面推理痕迹，丢失了深层的多分支结构。
method: 提出RLKD，用强化学习训练学生模型模拟教师的推理选择过程，而非简单预测词序列。
result: 在数学和常识推理任务上，RLKD提升了学生模型的推理准确性。
conclusion: 强化学习可更有效地蒸馏教师LLM的内在推理结构。
---

## Abstract
Distilling reasoning paths from teacher to student models via supervised fine-tuning (SFT) provides a shortcut for improving the reasoning ability of the smaller Large Language Models (LLMs). However, the reasoning paths generated by teacher models often reflect only surface-level traces of their underlying authentic reasoning. Insights from cognitive neuroscience suggest that authentic reasoning involves a complex interweaving between meta-reasoning that selects the appropriate sub-problem from multiple candidates, and solving, which addresses the sub-problem. It means that authentic reasoning has implicit multi-branch structure. Supervised fine-tuning collapses this rich structure into a flat sequence of token prediction in teacher's reasoning path, which cannot distill this structure to student. To address this limitation, we propose RLKD, a reinforcement learning (RL)-based distillation framework guided by a novel Generative Structure Reward Model (GSRM). Our GSRM converts the reasoning path into multiple meta-reasoning-solving steps and gives the reward to measure the alignment between the reasoning structures of student and teacher. Our RLKD combines this reward with RL, enables the student LLM to internalize the teacher’s implicit multi-branch structure in authentic reasoning, rather than merely mimicking fixed teacher's output paths. Experiments show that RLKD, even when trained on only 0.1% of the data under an RL-only regime, surpasses the performance of standard SFT-RL pipelines and further unleashes the potential reasoning ability of the student LLM than SFT-based distillation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大语言模型（LLM）在复杂推理任务上表现优异，但训练成本高、依赖强基座模型，资源受限团队难以复现。通过监督微调（SFT）蒸馏教师模型的推理路径是一种捷径，但SFT仅能复制表面推理痕迹，无法传递教师模型内在的“隐含多分支结构”——即真实推理中元推理（选择子问题）与求解（执行子问题）交织的动态过程。
- **核心问题**：SFT将丰富的推理结构压缩为扁平化的词序列预测，导致学生模型“盲目模仿”，无法学会从多个潜在路径中采样，从而限制了推理能力的真正提升。
- **整体含义**：本文指出，蒸馏必须传递推理的隐含多分支结构，而SFT无法胜任，因此提出首个基于强化学习（RL）的蒸馏框架RLKD，以弥补这一缺陷。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：利用强化学习训练学生模型，使其在推理过程中模仿教师的元推理-求解模式，而非简单预测输出词序列。通过设计结构化奖励，引导学生从多个潜在子问题中正确选择并求解。
- **关键技术细节**：
  - **生成式结构奖励模型（GSRM）**：两阶段模式，结合生成式奖励模型的语义理解与规则奖励模型的可解释性。
    - 第一阶段：训练一个LLM（基于Qwen2.5-7B-Instruct）将推理路径转化为元推理-求解步骤序列（M, Q&A对）。通过GPT-4o进行上下文学习自动构建93,625个训练样本，并使用DeepSeek-V3进行验证反馈。
    - 第二阶段：设计结构化奖励机制，顺序比较教师与学生的步骤序列，根据元推理匹配、问题匹配、答案匹配给予逐步奖励（见Algorithm 1）。
  - **RL训练框架**：将GSRM奖励与任务结果奖励（如数学准确性）加权结合，用于GRPO（Group Relative Policy Optimization）强化学习。无需SFT，直接用RL训练。
  - **结构化细粒度训练**：分阶段优化元推理和求解部分，动态调整不同任务（M vs Q&A）的损失权重。

## 3. 实验设计

- **数据集**：
  - 训练：使用OpenR1-Math（包含DeepSeek-R1生成的推理路径）作为教师路径来源；RLKD训练数据为OpenR1-Math中的3.2K样本（仅0.1%），与baseline SFT-RL使用的2,895K样本形成对比。
  - 评估：AIME24（数学），MATH-500（数学），GPQA-Diamond（研究生级问答）。
- **评估指标**：pass@1和pass@k（k=4,8,16,32,64），重点考察学生模型在多次采样中正确路径的探索能力。
- **对比方法**：
  - SFT-RL流水线：Qwen2.5-Math-7B-Instruct（SFT+GRPO训练）。
  - RL baseline：PPO和GRPO基于DeepSeek-R1-Distill-Qwen-7B。
  - SFT蒸馏对比：在领域偏移和域外设置下比较SFT vs RLKD。
- **消融实验**：比较RLKD与GRPO（去掉GSRM奖励）的训练动态（accuracy、完成长度、结构匹配度）。

## 4. 资源与算力

- 文中明确说明：
  - 硬件：使用1块Ascend 910B 64G NPU进行在线推理，4块Ascend 910B 64G NPU进行训练（Deepspeed-ZeRO3）。
  - 训练框架：PyTorch 2.5.1，Deepspeed 0.15.4。
  - 超参数：per-device batch size=2，梯度累积步数=4，GRPO group size=4，temperature=0.7。
- 未提及具体训练时长或总GPU小时数。

## 5. 实验数量与充分性

- **实验数量**：覆盖三个主要实验设置（Qwen2.5-Math基础模型、DeepSeek-R1-Distill-Qwen-7B基础模型、领域偏移/域外蒸馏比较），每组包含多个评估指标（pass@1 / pass@k）和多个训练步数点。
- **消融实验**：展示了RLKD vs GRPO在accuracy、完成长度、结构匹配度上的训练曲线（图3）。
- **充分性分析**：
  - 实验设计较为全面，考虑了不同基座模型、不同baseline、不同数据规模（0.1% vs 全量SFT数据）。
  - 评估pass@k以反映多分支探索能力，符合论文核心论证。
  - 领域偏移和域外设置下的对比（图4）有效证明了SFT的过度记忆与RLKD的泛化优势。
  - 多样性分析（图5）进一步验证了RLKD使学生模型的路径多样性更接近教师。
- **客观性与公平性**：报告了多次运行结果以减少随机性，关键指标（AIME24、MATH-500、GPQA）与公开baseline使用一致设置。但未提及是否进行统计显著性检验。

## 6. 主要结论与发现

- RLKD（仅需0.1%数据，RL-only）在AIME24上超越SFT-RL流水线（Qwen2.5-Math-7B-Instruct），表明其更高效地蒸馏了推理结构。
- 在SFT蒸馏模型（DeepSeek-R1-Distill-Qwen-7B）基础上，RLKD进一步显著提升性能，而PPO/GRPO改进有限，证明RLKD能捕获SFT遗漏的隐含结构。
- 领域偏移和域外实验中，RLKD性能持续提升，而SFT性能不断下降，说明SFT易陷入路径记忆，RLKD学会了可泛化的真实推理。
- 多样性分析显示，RLKD使学生模型的推理路径多样性逐渐接近教师，缩小了模仿与真实推理的差距。

## 7. 优点

- **方法论创新**：首次将强化学习引入推理蒸馏，并设计GSRM显式建模推理的隐含多分支结构，解决了SFT的扁平化问题。
- **数据高效**：仅用0.1%数据（3.2K样本）即可超越全量SFT-RL流水线，实用性强。
- **通用性**：适用于不同基座模型（Qwen2.5-Math-7B, DeepSeek-R1-Distill-Qwen-7B）和不同任务（数学、研究生级问答）。
- **深入分析**：通过领域偏移、域外、多样性分析等多角度实验，有力论证了SFT的局限与RLKD的优势。
- **开源贡献**：代码已公开。

## 8. 不足与局限

- **评估范围有限**：仅测试了数学和GPQA（研究生级问答），未涉及其他推理任务（如代码、科学推理），泛化性需进一步验证。
- **计算资源未详述**：未提供训练总时长或GPU小时数，复现成本不透明。
- **GSRM质量依赖**：依赖GPT-4o数据生成和DeepSeek-V3验证，可能引入标注偏差或质量波动；且GSRM本身需要额外训练。
- **统计显著性**：未报告置信区间或显著性检验，部分提升可能受随机性影响。
- **仅对比有限baseline**：RL baseline仅比较了PPO和GRPO，未与最新RL方法（如DAPO）对比。
- **应用限制**：RLKD需在线推理生成教师和学生序列，训练时计算开销高于SFT蒸馏，可能不适用于极低资源场景。

（完）
